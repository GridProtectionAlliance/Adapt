<UserControl x:Class="Adapt.View.DataSourceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Adapt.View.DataSource"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <TabControl TabStripPlacement="Top" Grid.Row="0">
            <TabItem Header="Settings">
                <local:SettingsView/>
            </TabItem>
            <TabItem Header="General MetaData">
                <local:MetaDataView/>
            </TabItem>
        </TabControl>
        <StackPanel Grid.Row="1"  Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Margin="10,0" Command="{Binding SaveCommand}">Save Changes</Button>
            <Button Margin="10,0" Command="{Binding TestCommand}">Test Data Source</Button>

        </StackPanel>
    </Grid>
   
</UserControl>
